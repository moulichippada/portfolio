var typed = new Typed(".text", {
    strings: ["Front End Developer", "Competative Programmer", "SQL Developer", "Data Analyst"],
    typeSpeed: 100,
    backSpeed: 100,
    backDelay: 1000,
    loop: true
});

function openCertificate(certificate) {
    window.open(certificate);
}
function opentab(certificate) {
    window.open(certificate);
}
